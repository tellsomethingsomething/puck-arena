[build]
builder = "NIXPACKS"
buildCommand = "pnpm build:shared && pnpm --filter @puck-arena/api build"

[deploy]
startCommand = "pnpm --filter @puck-arena/api start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
NODE_ENV = "production"
